import React from 'react'

function Modal(props) {
    // const {image, title} = props
  return (
    <> 
        <div className='border border-primary'><h2>{props.title}</h2></div>
    </>
  )
}

export default Modal